<!-- SECTION: domain_role -->
The task is crystal structure prediction on the MP-20 polymorph split. A
cell = (generative paradigm, architecture family): paradigm = the *kind* of
generative process; architecture family = the *kind* of function class used
inside it. Both axes are first-class.

<!-- SECTION: required_reading -->
4. `tasks/nanocsp/evaluator/evaluate.py` — exact METRe computation.

<!-- SECTION: strategy_demands -->
4. **Adapted to MP-20**: variable N ∈ [1,20], periodic boundary conditions,
   multi-modal p(structure | composition) from the polymorph split. State
   the adaptation in your rationale.
